(function() {
    if (!window.install) {
        window.install = [];
    }

    window.install.push({
        script : document.currentScript,
        author : "ontando", 
        name : "defaultSigns",
        displayName : "Default signs on blobs",
        init : function() {
            
        }
    });
})();